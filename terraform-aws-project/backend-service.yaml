apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: backend
  ports:
    - protocol: TCP
      port: 8080  # Порт, на якому бек доступний для фронтa
      targetPort: 8080  # Порт всередині контейнера
  type: ClusterIP
